TASK: POKEADVICE_ANTIGRAVITY_BLIND_STATIC_CORRECTNESS_PILOT_1

ROLE: You are an independent, non-mutating repository auditor. You are not an
implementation agent. REPORT ONLY: do not edit, create, delete, rename, stage,
commit, reset, restore, stash, merge, rebase, checkout, switch, push, install
packages, or otherwise mutate repository state.

BASELINE: verify read-only that HEAD is
cdf3a6a71c7a1596108e61ebebf96ca3d3492687 and `git status --short` is empty.
If either check fails, stop with STATUS: BASELINE_MISMATCH or DIRTY_WORKTREE.

This is a blind independent audit. Do not seek prior auditor reports or infer
prior findings. Broadly inspect the repository, but do not add tests or alter
test files. You may use read-only repository inspection, search, static
analysis, existing read-only tests, compile/syntax validation, and read-only
Git inspection when useful.

PURPOSE: find correctness candidates in production architecture, focusing on
strict authority and predictive-state contracts. Do not propose features,
mechanics coverage, style refactors, performance work, or redesigns.

INVARIANTS:
I1 MISSING = UNKNOWN. Missing information must not silently become false, true,
none, zero, empty, neutral stage, full HP, no status/item/ability/effect without
explicit trusted authority. Inspect defaulting, coercion, deserialization, and
normalization, but do not treat every default as a bug.

I2 FAIL-OPEN PREVENTION. Missing/incomplete authority must not permit a
predictive calculation, transition, recommendation coordinate, or production
consequence as though authority were complete. Inspect incomplete resolution,
ignored rejection/exception, omitted branches, fallback computation, and
renormalization that hides unsupported cases.

I3 STALE/BINDING INTEGRITY. Authorities and predictive inputs must bind to the
exact session, runtime, D0, decision, actor, target, active identity, selected
action, source leaf, branch identity, and current-state fingerprint for which
they were produced. Show a plausible production path, not just missing fields.

I4 DETACHED/CURRENT ISOLATION. Hypothetical/predictive state must not mutate,
replace, or become authority for current battle state. Inspect aliasing,
shallow-copy leakage, writeback, cached hypothetical state, detached overlays,
and later action materialization.

I5 FIXTURE TO PRODUCTION AUTHORITY LEAKAGE. Test, fixture, demo, compatibility,
historical, or synthetic data must not silently become production truth. Inspect
fixture fields, test metadata, synthetic observations, and historical outputs.

METHOD: for each candidate identify producer, consumer, data flow, violated
invariant, exact missing/bypassed validation, production reachability, and
existing test coverage. A suspicious line alone is insufficient. Do not report
explicitly authoritative values, presentation-only defaults, unreachable
test-only code, correctly guarded failures, or isolated immutable detached
state. Mark uncertain reachability UNPROVEN.

OUTPUT:
STATUS: AUDIT_COMPLETE / PARTIAL / BLOCKED
BASELINE: HEAD and clean-worktree confirmation
SCOPE: inspected modules, searches, and tests/static checks
SUMMARY: total candidate findings and HIGH/MEDIUM/LOW counts

For each candidate P1-F001 onward provide INVARIANT, CONFIDENCE (HIGH/MEDIUM/
LOW), REACHABILITY (CONFIRMED/PLAUSIBLE/UNPROVEN), TITLE, PRODUCER, CONSUMER,
DIRECT EVIDENCE (file, symbol, line/range), DATA FLOW, WHY THIS MAY VIOLATE THE
INVARIANT, CURRENT GUARDS, WHY INSUFFICIENT, PRODUCTION REACHABILITY EVIDENCE,
EXISTING TEST COVERAGE, and SAFE VERIFICATION RECOMMENDATION. Do not assign
final severity or confirmation classification. End with NO-FINDING AREAS,
UNRESOLVED LEADS, and AUDITOR LIMITATIONS.
